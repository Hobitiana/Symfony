{% extends 'base.html.twig' %}

{% block title %}Activite1 index{% endblock %}

{% block body %}
    <h1>Activite1 index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for activite1 in activite1s %}
            <tr>
                <td>{{ activite1.id }}</td>
                <td>{{ activite1.nom }}</td>
                <td>
                    <a href="{{ path('app_activite1_crud_show', {'id': activite1.id}) }}">show</a>
                    <a href="{{ path('app_activite1_crud_edit', {'id': activite1.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_activite1_crud_new') }}">Create new</a>
{% endblock %}
