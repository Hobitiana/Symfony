{# {% extends "baseDashboard.html.twig" %} 
{% block title %}type de Construction{% endblock %}

{% block  content %}
{{ form_start(form) }}


    <button type="submit" class="btn btn-primary">Enregistrer</button>

{{ form_end(form) }}
<a href="{{ path('affichage_NatureProjet') }} ">Nature du projet</a>
{% endblock %} #}
{% extends "baseDashboard.html.twig" %}

{% block title %}Type de l'entreprise{% endblock %}

{% block content %}
<div class="main-content container-fluid">
    {{ form_start(form) }}

    <section id="horizontal-input">
        <!-- Section pour le formulaire -->
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">
                    <h4 class="card-title">Type de l'entreprise</h4>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-12">
                            <div class="form-group row align-items-center">
                                <div class="col-lg-2 col-3">
                                    {{ form_label(form.typeEntrprise, 'Entreprise', {'label_attr': {'class': 'col-form-label'}}) }}
                                </div>
                                <div class="col-lg-10 col-9">
                                    {{ form_widget(form.typeEntrprise, {'attr': {'class': 'form-select'}}) }}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

   <div class="row mt-3">
    <div class="col-md-12 text-end">
        <button type="submit" class="btn btn-primary">Enregistrer</button>
    </div>
    </div>
    {{ form_end(form) }}
</div>
{% endblock %}

