{% extends 'base.html.twig' %}

{% block title %}Nationalite index{% endblock %}

{% block body %}
    <h1>Nationalite index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NomNationalite</th>
                <th>CodeNationalite</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for nationalite in nationalites %}
            <tr>
                <td>{{ nationalite.id }}</td>
                <td>{{ nationalite.nomNationalite }}</td>
                <td>{{ nationalite.CodeNationalite }}</td>
                <td>
                    <a href="{{ path('app_nationalite_crud_show', {'id': nationalite.id}) }}">show</a>
                    <a href="{{ path('app_nationalite_crud_edit', {'id': nationalite.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_nationalite_crud_new') }}">Create new</a>
{% endblock %}
