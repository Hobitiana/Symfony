{% extends 'base.html.twig' %}

{% block title %}ResultatDemande{% endblock %}

{% block body %}
    <h1>ResultatDemande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ resultat_demande.id }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ resultat_demande.reference }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_resultat_demande_index') }}">back to list</a>

    <a href="{{ path('app_resultat_demande_edit', {'id': resultat_demande.id}) }}">edit</a>

    {{ include('resultat_demande/_delete_form.html.twig') }}
{% endblock %}
