<form method="post" action="{{ path('app_responsable_demande_crud_delete', {'id': responsable_demande.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ responsable_demande.id) }}">
    <button class="btn">Delete</button>
</form>
