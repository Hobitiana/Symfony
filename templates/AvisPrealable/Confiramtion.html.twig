{% extends "baseDashboard.html.twig" %}

{% block title %}Confirmation{% endblock %}

{% block content %}
<div class="main-content container-fluid">


    <section id="horizontal-input">
        <!-- Section pour le formulaire -->
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">
                    <h4 class="card-title">Confirmation de donnee</h4>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-12">
                            <div class="form-group row align-items-center">
                                <form method="post" action="{{ path('enregistrement_mes_donnees') }}">
                                 
                                    <button type="submit" name="action" value="save" class="btn btn-primary">Enregistrer</button>
                                    <button type="submit" name="action" value="cancel" class="btn btn-secondary">Annuler</button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Section des boutons Enregistrer et Groupe activité -->
   

</div>
{% endblock %}
