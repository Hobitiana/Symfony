{% extends 'base.html.twig' %}

{% block title %}Activite1{% endblock %}

{% block body %}
    <h1>Activite1</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ activite1.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ activite1.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_activite1_crud_index') }}">back to list</a>

    <a href="{{ path('app_activite1_crud_edit', {'id': activite1.id}) }}">edit</a>

    {{ include('activite1_crud/_delete_form.html.twig') }}
{% endblock %}
