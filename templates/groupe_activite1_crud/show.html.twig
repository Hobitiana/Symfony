{% extends 'base.html.twig' %}

{% block title %}GroupeActivite1{% endblock %}

{% block body %}
    <h1>GroupeActivite1</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ groupe_activite1.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ groupe_activite1.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_groupe_activite1_crud_index') }}">back to list</a>

    <a href="{{ path('app_groupe_activite1_crud_edit', {'id': groupe_activite1.id}) }}">edit</a>

    {{ include('groupe_activite1_crud/_delete_form.html.twig') }}
{% endblock %}
